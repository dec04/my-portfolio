name: Greetings

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: 'Oh, how nice. Your first issue.'' first issue'
        pr-message: 'Hi, my nickname is Dec04. Sometimes i develope some stuff. Let me show you...'' first pr'
